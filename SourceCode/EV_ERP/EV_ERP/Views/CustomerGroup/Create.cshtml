@model EV_ERP.Models.ViewModels.Customers.CustomerGroupFormViewModel
@{
    ViewData["Title"] = "Thêm nhóm khách hàng";
    ViewData["Breadcrumbs"] = new List<BreadcrumbItem>
    {
        new("Trang chủ", "/"),
        new("Nhóm khách hàng", "/CustomerGroup"),
        new("Tạo mới")
    };
}

<div class="row justify-content-center">
    <div class="col-xl-6 col-lg-8">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white border-bottom py-3">
                <h5 class="mb-0 fw-semibold">
                    <i class="bi bi-plus-circle me-2 text-primary"></i>Thêm nhóm khách hàng
                </h5>
            </div>
            <div class="card-body p-4">
                <form asp-action="Create" asp-controller="CustomerGroup" method="post">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small"></div>

                    @await Html.PartialAsync("_GroupForm", Model)

                    <hr class="my-4" />
                    <div class="d-flex gap-2 justify-content-end">
                        <a href="/CustomerGroup" class="btn btn-outline-secondary">Hủy</a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-lg me-1"></i> Tạo nhóm
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
