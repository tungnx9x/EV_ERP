@model EV_ERP.Models.ViewModels.Customers.CustomerGroupFormViewModel
@{
    ViewData["Title"] = "Chỉnh sửa nhóm khách hàng";
    ViewData["Breadcrumbs"] = new List<BreadcrumbItem>
    {
        new("Trang chủ", "/"),
        new("Nhóm khách hàng", "/CustomerGroup"),
        new("Chỉnh sửa")
    };
}

<div class="row justify-content-center">
    <div class="col-xl-6 col-lg-8">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white border-bottom py-3">
                <h5 class="mb-0 fw-semibold">
                    <i class="bi bi-pencil-square me-2 text-primary"></i>Chỉnh sửa nhóm khách hàng
                </h5>
            </div>
            <div class="card-body p-4">
                <form asp-action="Edit" asp-controller="CustomerGroup" method="post">
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="CustomerGroupId" />
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2 small"></div>

                    @await Html.PartialAsync("_GroupForm", Model)

                    <hr class="my-4" />
                    <div class="d-flex gap-2 justify-content-end">
                        <a href="/CustomerGroup" class="btn btn-outline-secondary">Hủy</a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-lg me-1"></i> Lưu thay đổi
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
